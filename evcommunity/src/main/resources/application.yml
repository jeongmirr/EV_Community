spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/temp?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: # 본인 mysql password 넣으시면 됩니다!


  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true